export const TIMELINE_BEHAVIOR = {
  selection: {
    marqueeTool: 'select',
  },
  resize: {
    defaultDirection: 1,
  },
}

export const TIMELINE_RECORDING_SELECTOR = {
  pageSize: 20,
  defaultKindFilter: 'all',
  defaultVisibilityFilter: 'public',
}
